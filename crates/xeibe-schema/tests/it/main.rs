//! Tests for `xeibe-schema`: the path tree, value statistics, the rule engine
//! and binding a given schema.
//!
//! The specification is `docs/schema-inference.md` and `docs/type-mapping.md`.

mod bind;
mod node;
mod pattern;
mod presets;
mod rules;
mod samples;
mod scan;
mod support;
mod value;
