//! Tests for `xeibe-core`: namespaces, the XML reader, decoding, sources, zip
//! archives, the feature splitter and GML version detection.
//!
//! The bodies of the crate are still `todo!()`, so these tests describe the
//! behaviour `docs/architecture.md` specifies rather than what exists.

mod archive;
mod decode;
mod namespace;
mod reader;
mod samples;
mod splitter;
mod support;
mod version;
