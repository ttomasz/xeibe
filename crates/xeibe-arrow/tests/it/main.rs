//! Tests for `xeibe-arrow`: `scan`, `read`, the settings file, lists and their
//! alignment, data that doesn't fit the schema, and reads of the real samples.

mod api;
mod lists;
mod mismatch;
mod samples;
mod settings;
mod support;
